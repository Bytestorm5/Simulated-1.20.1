/**
 * Stand-ins for Minecraft 1.21 types that Simulated, Aeronautics and Offroad were written against and that Forge 1.20.1
 * (or Veil's {@code foundry.veil.backport} package) doesn't provide. They keep the 1.21 names and API, so the code that
 * uses them reads the same as upstream.
 */
package dev.simulated_team.simulated.backport;
